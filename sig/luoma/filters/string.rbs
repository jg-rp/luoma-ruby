module Luoma
  module Filters
    # Return _left_ concatenated with _right_.
    # Coerce _left_ and _right_ to strings if they aren't strings already.
    def self.append: (untyped left, untyped right, context: FilterContext) -> untyped

    # Return _left_ with the first character in uppercase and the rest lowercase.
    # Coerce _left_ to a string if it is not one already.
    def self.capitalize: (untyped left, context: FilterContext) -> untyped

    # Return _left_ with all characters converted to lowercase.
    # Coerce _left_ to a string if it is not one already.
    def self.downcase: (untyped left, context: FilterContext) -> untyped

    # Return _left_ with all characters converted to uppercase.
    # Coerce _left_ to a string if it is not one already.
    def self.upcase: (untyped left, context: FilterContext) -> untyped

    # Return _left_ with special HTML characters replaced with their HTML-safe escape sequences.
    # Coerce _left_ to a string if it is not one already.
    def self.escape: (untyped left, context: FilterContext) -> (untyped | nil)

    # Return _left_ with special HTML characters replaced with their HTML-safe escape sequences.
    # Coerce _left_ to a string if it is not one already.
    #
    # It is safe to use `escape_once` on string values that already contain HTML-escape sequences.
    def self.escape_once: (untyped left, context: FilterContext) -> untyped

    # Return _left_ with leading whitespace removed.
    # Coerce _left_ to a string if it is not one already.
    def self.lstrip: (untyped left, context: FilterContext) -> untyped

    # Return _left_ with trailing whitespace removed.
    # Coerce _left_ to a string if it is not one already.
    def self.rstrip: (untyped left, context: FilterContext) -> untyped

    # Return _left_ with leading and trailing whitespace removed.
    # Coerce _left_ to a string if it is not one already.
    def self.strip: (untyped left, context: FilterContext) -> untyped

    # Return _left_ with LF or CRLF replaced with `<br />\n`.
    def self.newline_to_br: (untyped left, context: FilterContext) -> untyped

    # Return _right_ concatenated with _left_.
    # Coerce _left_ and _right_ to strings if they aren't strings already.
    def self.prepend_: (untyped left, untyped right, context: FilterContext) -> untyped

    # Return _left_ with all occurrences of _pattern_ replaced with _replacement_.
    # All arguments are coerced to strings if they aren't strings already.
    def self.replace: (untyped left, untyped pattern, ?::String replacement, context: FilterContext) -> untyped

    # Return _left_ with the first occurrence of _pattern_ replaced with _replacement_.
    # All arguments are coerced to strings if they aren't strings already.
    def self.replace_first: (untyped left, untyped pattern, ?::String replacement, context: FilterContext) -> untyped

    # Return _left_ with the last occurrence of _pattern_ replaced with _replacement_.
    # All arguments are coerced to strings if they aren't strings already.
    def self.replace_last: (untyped left, untyped pattern, untyped replacement, context: FilterContext) -> untyped

    # Return _left_ with all occurrences of _pattern_ removed.
    # All arguments are coerced to strings if they aren't strings already.
    def self.remove: (untyped left, untyped pattern, context: FilterContext) -> untyped

    # Return _left_ with the first occurrence of _pattern_ removed.
    # All arguments are coerced to strings if they aren't strings already.
    def self.remove_first: (untyped left, untyped pattern, context: FilterContext) -> untyped

    # Return _left_ with the last occurrence of _pattern_ removed.
    # All arguments are coerced to strings if they aren't strings already.
    def self.remove_last: (untyped left, untyped pattern, context: FilterContext) -> untyped

    # Split _left_ on every occurrence of _pattern_.
    def self.split: (untyped left, untyped pattern, context: FilterContext) -> untyped

    RE_HTML_BLOCKS: ::Regexp

    RE_HTML_TAGS: ::Regexp

    # Return _left_ with HTML tags removed.
    def self.strip_html: (untyped left, context: FilterContext) -> untyped

    # Return _left_ with CR and LF removed.
    def self.strip_newlines: (untyped left, context: FilterContext) -> untyped

    def self.truncate: (untyped left, ?::Integer max_length, ?::String ellipsis, context: FilterContext) -> untyped

    def self.truncatewords: (untyped left, ?::Integer max_words, ?::String ellipsis, context: FilterContext) -> untyped

    def self.url_encode: (untyped left, context: FilterContext) -> untyped

    def self.url_decode: (untyped left, context: FilterContext) -> untyped

    def self.base64_encode: (untyped left, context: FilterContext) -> untyped

    def self.base64_decode: (untyped left, context: FilterContext) -> untyped

    def self.base64_url_safe_encode: (untyped left, context: FilterContext) -> untyped

    def self.base64_url_safe_decode: (untyped left, context: FilterContext) -> untyped
  end
end