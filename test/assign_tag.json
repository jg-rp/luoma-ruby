{
  "tests": [
    {
      "name": "string literal",
      "template": "{% assign foo = 'foo' %}{{ foo }}",
      "result": "foo"
    },
    {
      "name": "string literal, filtered",
      "template": "{% assign foo = 'foo' | upcase %}{{ foo }}",
      "result": "FOO"
    },
    {
      "name": "shadow global variable",
      "template": "{{ foo }}{% assign foo = 'foo' | upcase %}{{ foo }}",
      "data": {
        "foo": "bar"
      },
      "result": "barFOO"
    },
    {
      "name": "range literal",
      "template": "{% assign foo = (1..3) %}{{ foo | join: '#' }}",
      "result": "1#2#3"
    },
    {
      "name": "extra whitespace",
      "template": "{% assign\nfoo\n= 'foo' %}{{ foo }}",
      "result": "foo"
    },
    {
      "name": "multiple, comma separated",
      "template": "{% assign\n  foo = 'foo',\n  bar = 2 %}{{ foo }} {{ bar }}",
      "result": "foo 2"
    },
    {
      "name": "multiple, comma separated, filtered",
      "template": "{% assign\n  foo = 'foo' | upcase ,\n  bar = 2 | plus: 5 %}{{ foo }} {{ bar }}",
      "result": "FOO 7"
    },
    {
      "name": "multiple, comma separated, filtered, parens",
      "template": "{% assign bar = (2 | plus: 5), foo = 'foo' | upcase %}{{ foo }} {{ bar }}",
      "result": "FOO 7"
    },
    {
      "name": "multiple, scope",
      "template": "{% assign foo = 1, bar = foo + 1 %}{{ foo }} {{ bar }}",
      "result": "1 2"
    },
    {
      "name": "expression",
      "template": "{% assign squash = (x, y) => x | strip | split: ' ' | join: (y or '-') %}{{ s | squash }} {{ s | squash: '*' }} {{ s | squash: ' ' }}",
      "data": { "s": "    foo    bar   " },
      "result": "foo-bar foo*bar foo bar"
    },
    {
      "name": "expression, rendered",
      "template": "{% assign squash = (x, y) => x | strip | split: ' ' | join: (y or '-') %}{{ squash }}",
      "result": "(x, y) => x | strip | split: \" \" | join: (y or \"-\")"
    },
    {
      "name": "expression, excess arguments are ignored",
      "template": "{% assign squash = (x, y) => x | strip | split: ' ' | join: (y or '-') %}{{ s | squash: '^', 'foo' }}",
      "data": { "s": "    foo    bar   " },
      "result": "foo^bar"
    },
    {
      "name": "expression, missing arguments are null",
      "template": "{% assign squash = (x, y, z) => x | strip | split: ' ' | join: (y or z or '-') %}{{ s | squash }}",
      "data": { "s": "    foo    bar   " },
      "result": "foo-bar"
    }
  ]
}
